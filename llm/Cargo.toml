[package]
name = "llm"
version = "0.1.0"
edition = "2024"

[dependencies]
tensor = { path = "../tensor" }
backend = { path = "../backend" }
model = { path = "../model" }
tokenizer = {path = "../tokenizer"}
serde = { version = "1", features = ["derive"] }
serde_json = "1"
